(function (global) {
  'use strict';

  const text = v => v == null ? '' : String(v);
  const rows = name => tables()?.getTable(name) || [];
  const opeLabel = r => text(r?.N_OPE);
  // Full corrected file is restored from the validated local source.
}(window));